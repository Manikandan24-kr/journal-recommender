# AWS Deployment Credentials
# Copy this file to .env.deploy and fill in your values
# NEVER commit .env.deploy to version control

# AWS Credentials
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_DEFAULT_REGION=us-east-1

# AWS Account ID (found in AWS Console or via: aws sts get-caller-identity)
AWS_ACCOUNT_ID=719117793522

# ECR Repository Names
ECR_BACKEND_REPO=journal-recommender-backend
ECR_FRONTEND_REPO=journal-recommender-frontend

# ECS Configuration
ECS_CLUSTER=journal-recommender-cluster
ECS_SERVICE_BACKEND=journal-recommender-backend
ECS_SERVICE_FRONTEND=journal-recommender-frontend

# RDS Database Configuration
# Using existing RDS instance: bibmanager-beta-db
RDS_HOST=bibmanager-beta-db.cw2h9tacjwrd.us-east-1.rds.amazonaws.com
RDS_PORT=5432
RDS_DATABASE=journal_recommender
RDS_USERNAME=your-db-username
RDS_PASSWORD=your-db-password

# ALB Configuration (using existing ALB)
ALB_NAME=bibmanager-alb

# Application Configuration
API_URL=https://journal-recommender.yourdomain.com
