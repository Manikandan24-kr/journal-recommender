databases:
  - name: journal-db
    plan: free
    databaseName: journal_recommender
    user: journal_user

services:
  # Backend API
  - type: web
    name: journal-api
    runtime: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: journal-db
          property: connectionString
      # OPENAI_API_KEY - Not required currently (keyword-based matching is used)
      # Uncomment below if you add LLM-powered recommendations in the future
      # - key: OPENAI_API_KEY
      #   sync: false
    healthCheckPath: /api/health

  # Frontend
  - type: web
    name: journal-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        fromService:
          name: journal-api
          type: web
          property: host
